@model IEnumerable<properTech.Models.Building>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BuildingName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a asp-action="Index" asp-route-id="@item.BuildingId">@item.BuildingName.</a>
                </td>
                <td>
                    <a asp-controller="Building" asp-action="Edit" asp-route-id="@item.BuildingId">Edit</a> |
                    <a asp-controller="Building" asp-action="Details" asp-route-id="@item.BuildingId">Details</a> |
                    <a asp-controller="Building" asp-action="Delete" asp-route-id="@item.BuildingId">Delete</a> |
                    <a asp-controller="Building" asp-action="Create" asp-route-id="@item.PropertyId">Create New Building</a>
                </td>
            </tr>
        }
    </tbody>
</table>
